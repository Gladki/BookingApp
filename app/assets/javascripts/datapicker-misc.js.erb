 $(document).ready(function() {

$('#uwaga-btn').button('');

    // uwaga-btn
 $('#uwaga-btn').on('click',function(){
 $("#uwagi").toggle('1000');
});
  // $('a rel#edit').tooltip();
  // $('a rel#show').tooltip();
  // $('a rel#delete').tooltip();
  // $('a rel#add').tooltip();
  // $('a rel#list').tooltip();


 

    $('#wycieczka_data').datepicker({
      language: 'pl',
      format: 'dd-mm-yyyy'
      });
    $('#rezerwacja_dataUrodzenia').datepicker({
      language: 'pl',
      defaultDate: "1990-01-01",
      format: 'dd-mm-yyyy',

      });

  });

